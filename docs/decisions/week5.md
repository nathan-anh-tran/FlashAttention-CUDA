- Doing softmax: softmax(x_i) = exp(x_i) / sum_j(exp(x_j)), turns a vector into a probability distribution (all values in [0,1], summing to 1)
- Numerical stability: naive softmax overflows for even moderately large inputs (exp(x) overflows float32 for x > ~88). Fix is the subtract-the-max trick: softmax(x_i) = exp(x_i - max(x)) / sum_j(exp(x_j - max(x))). Mathematically an exact identity (multiplying num/denom of a fraction by exp(-max) doesn't change its value), not an approximation — confirmed this isn't "rough," it's provably equal in infinite precision, and in float32 practice it's the more accurate version, not less.
- Reference convention shifted from Weeks 1-4: wrote src/reference/softmax.py using torch.softmax as ground truth (per project environment conventions), instead of a hand-written C++ reference. Test input mixes normal-range values with two large, close-together values (1000.0, 1000.5) specifically to stress-test overflow safety — a naive softmax on this input would produce NaN.
- Wrote src/kernels/softmax.cu: single block (n=8, threadsPerBlock=8), combining two tree-reductions in sequence inside one kernel — max-reduce, then sum-reduce — reusing the same sdata shared-memory buffer for both, plus two elementwise steps (subtract max + expf, then divide).
- New pattern learned: reusing shared memory across sequential reduction phases within one kernel. Per-thread values that are needed *after* a reduction (original input value, and the exp'd value) must be saved into local registers before the reduction starts, since the tree-reduction overwrites most threads' sdata slots. Reading a reduction's result back out (e.g. rowMax, rowTotal) needs no special "broadcast" step — every thread can just read sdata[0] directly, since sdata is already block-shared; the reduction loop's last __syncthreads() already guarantees visibility.
- Bugs hit along the way (all fixed):
  - Missing __syncthreads() between writing expVal into sdata and starting the sum-reduction loop — same race-condition class as Week 4's first kernel, easy to drop when chaining a second reduction phase.
  - CPU-reference scoping bug: `float rowMax = max(rowMax, h_in[i]);` inside a for-loop redeclared a new block-scoped variable each iteration instead of updating the outer one, so the outer rowMax stayed stuck at -INFINITY. A real gotcha coming from Python, where loop bodies don't introduce a new scope the way C++/Java `{}` blocks do.
  - CPU reference initially undersized its exp-value array (`new float` instead of `new float[n]` / a fixed array) and initially omitted the max-subtraction entirely — would have reproduced the exact overflow bug the kernel was built to avoid, inside the "reference" meant to catch it.
- CPU reference condensed per project convention (matches how vector_add.cu/reduction files validate): compute expected values inline via a serial CPU loop rather than hardcoding literals from softmax.py's output. Deferred the final normalization (divide by sum) into the comparison loop itself rather than a separate pass, since the division only needs the already-known total.
- Validated: fabsf(expected - h_out[i]) < 1e-5 for all i, matches project convention. Ran on Colab T4 (sm_75). Passed.
- Profiled with ncu out of curiosity (n=8, 32 bytes total) — Memory Throughput 1.19 GB/s, Mem Busy 0.74%, Max Bandwidth 0.61%, L1/TEX Hit Rate 0%, L2 Hit Rate 87.58%, Mem Pipes Busy 0.04%. All near zero as expected — echoes the Week 4 lesson that a too-small workload gives no real memory signal. Not a concern: Week 5's milestone is numerical correctness, not performance, so no profiler evidence is required here the way it was for reduction.
- Week 5 milestone met: correct, numerically-stable softmax matching a real (PyTorch) reference within float tolerance.
- TODO next: generalize from single-vector (one block, n=8) to row-wise softmax over a matrix (one block per row) — the natural on-ramp into Week 6's naive attention, which needs softmax applied independently across each row of the QK^T matrix.
